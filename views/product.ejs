<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product</title>
  <!-- Include Tailwind CSS via CDN -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/logoutbtn.css">
  <style>
    body {
      background-color: #374151;
    }

    input {
      border: none
    }

    .mainPictureContainer {
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .mainPictureContainer img {
      max-height: 100%;
      width: auto;
      object-fit: contain;
    }

    .product-badge {
      background-color: #4F46E5;
      color: white;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 500;
    }

    .discount-badge {
      background-color: #059669;
      color: white;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 500;
    }
  </style>
  <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.7.3/dist/alpine.min.js"></script>
</head>

<body>
  <nav class="bg-gray-800">
    <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
      <div class="relative flex h-16 items-center justify-between">
        <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
          <!-- Mobile menu button-->
          <button type="button"
            class="relative inline-flex items-center justify-center rounded-md p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white"
            aria-controls="mobile-menu" aria-expanded="false">
            <span id="menuToggle" class="absolute -inset-0.5"></span>
            <script>
              document.getElementById("menuToggle").addEventListener("click", () => {
                document.getElementById("mobileMenu").classList.toggle("show")
              })
            </script>
            <style>
              #mobileMenu,
              #profiledropdown {
                height: 0;
                margin: 0;
                padding: 0;
                overflow: hidden;
              }

              .show {
                height: auto !important;

                transition: all 1s ease-out;
              }
            </style>
            <span class="sr-only">Open main menu</span>
            <!--
            Icon when menu is closed.

            Menu open: "hidden", Menu closed: "block"
          -->
            <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"
              aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
            </svg>
            <!--
            Icon when menu is open.

            Menu open: "block", Menu closed: "hidden"
          -->
            <svg class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"
              aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
        <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
          <div class="flex flex-shrink-0 items-center">
            <img class="h-8 w-auto" src="/marketingtool-logo2.png"
              alt="Your Company">
          </div>
          <div class="hidden sm:ml-6 sm:block">
            <div class="flex space-x-4">
              <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
              <a href="/" class=" bg-gray-900 text-white rounded-md px-3 py-2 text-sm font-medium aria-current="
                page">Home</a>
              <a href="/services"
                class="text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium">Services</a>
              <a href="/career"
                class="text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium">Career</a>
              <a href="/aboutus"
                class="text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium"">About Us</a>
          <a href=" /contactus"
                class="text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">Contact
                Us</a>
                <% if (loggedIn) { %>
                  <a class="logoutContainer" href="/logout">
                    <button class="Btn">
                      <div class="sign"><svg viewBox="0 0 512 512">
                          <path d="M377.9 105.9L500.7 228.7c7.2 7.2 11.3 17.1 11.3 27.3s-4.1 20.1-11.3 27.3L377.9 406.1c-6.4 6.4-15 9.9-24 9.9c-18.7 0-33.9-15.2-33.9-33.9l0-62.1-128 0c-17.7 0-32-14.3-32-32l0-64c0-17.7 14.3-32 32-32l128 0 0-62.1c0-18.7 15.2-33.9 33.9-33.9c9 0 17.6 3.6 24 9.9zM160 96L96 96c-17.7 0-32 14.3-32 32l0 256c0 17.7 14.3 32 32 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32l-64 0c-53 0-96-43-96-96L0 128C0 75 43 32 96 32l64 0c17.7 0 32 14.3 32 32s-14.3 32-32 32z">
                          </path>
                        </svg></div>
                      <div class="text">Logout</div>
                    </button>
                  </a>
                  <% } else { %>
                  <a href="/login" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Log
                    In</a>
                  <a href="/signupSelect" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">Sign Up</a>
                  <% } %>
            </div>
          </div>
        </div>
        <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
          <!-- Profile dropdown -->




        </div>
      </div>
    </div>
    </div>

    <!-- Mobile menu, show/hide based on menu state. -->
    <div class="sm:hidden" id="mobile-menu">
      <div id="mobileMenu" class="space-y-1 px-2 pb-3 pt-2 ">
        <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
        <a href="/" class="bg-gray-900 text-white block rounded-md px-3 py-2 text-base font-medium"
          aria-current="page">Home</a>
        <a href="/services"
          class="text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">Services</a>
        <a href="/career"
          class="text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">Career</a>
        <a href="/aboutus"
          class="text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">About
          Us</a>
        <a href="/contactus"
          class="text-gray-300 hover:bg-gray-700 hover:text-white block rounded-md px-3 py-2 text-base font-medium">Contact
          Us</a>


      </div>
    </div>
  </nav>

  <div class="antialiased">
    <div class="py-6">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-6">
        <div class="flex flex-col md:flex-row -mx-4">
          <div class="md:flex-1 px-4">
            <div x-data="{ image: 1 }" x-cloak>
              <div class="h-64 md:h-80 rounded-lg bg-gray-100 mb-4">
                <div x-show="image === 1"
                  class="mainPictureContainer h-64 md:h-80 rounded-lg bg-gray-100 mb-4 flex items-center justify-center">
                  <!--               <span class="text-5xl">1</span> -->
                  <img class="rounded-lg"
                    src="<%= product.mainImage ? '/' + product.mainImage : 'https://i.ibb.co/kSZwvxX/addLogo.png' %>">
                </div>

                <div x-show="image === 2"
                  class="mainPictureContainer h-64 md:h-80 rounded-lg bg-gray-100 mb-4 flex items-center justify-center">
                  <img class="rounded-lg"
                    src="<%= product.image1 ? '/' + product.image1 : 'https://i.ibb.co/kSZwvxX/addLogo.png' %>">
                </div>

                <div x-show="image === 3"
                  class="mainPictureContainer h-64 md:h-80 rounded-lg bg-gray-100 mb-4 flex items-center justify-center">
                  <img class="rounded-lg"
                    src="<%= product.image2 ? '/' + product.image2 : 'https://i.ibb.co/kSZwvxX/addLogo.png' %>">
                </div>

                <div x-show="image === 4"
                  class="mainPictureContainer h-64 md:h-80 rounded-lg bg-gray-100 mb-4 flex items-center justify-center">
                  <img class="rounded-lg"
                    src="<%= product.image3 ? '/' + product.image3 : 'https://i.ibb.co/kSZwvxX/addLogo.png' %>">
                </div>
              </div>

              <div class="flex -mx-2 mb-4">
                <template x-for="i in 3">
                  <div class="flex-1 px-2">
                    <button x-on:click="image = i" :class="{ 'ring-2 ring-indigo-300 ring-inset': image === i }"
                      class="focus:outline-none w-full rounded-lg h-24 md:h-32 bg-gray-100 flex items-center justify-center">
                      <span x-text="i" class="text-2xl"></span>
                    </button>
                  </div>
                </template>
              </div>
            </div>
          </div>
          <div class="md:flex-1 px-4">
            <div class="flex items-center gap-2 mb-4">
              <h2 class="leading-tight tracking-tight font-bold text-white text-2xl md:text-3xl">
                <%= product.name %>
              </h2>
              <span class="product-badge"><%= product.make %></span>
            </div>
            
            <p class="text-white text-sm mb-4">By 
              <span class="text-yellow-600 hover:underline"><%= createdBy.name %></span>
            </p>

            <div class="flex items-center space-x-4 my-6">
              <div>
                <div class="rounded-lg bg-gray-100 flex py-2 px-3">
                  <span class="text-indigo-400 mr-1 mt-1">₹</span>
                  <span class="font-bold text-indigo-600 text-3xl"><%= product.offer %></span>
                </div>
              </div>
              <div class="flex-1">
                <p class="text-gray-400 text-xl font-semibold line-through">₹<%= product.price %></p>
                <div class="flex items-center gap-2">
                  <span class="discount-badge">
                    <%= Math.round((product.price - product.offer) / product.price * 100) %>% OFF
                  </span>
                  <p class="text-gray-400 text-sm">Inclusive of all Taxes</p>
                </div>
              </div>
            </div>

            <!-- Product details -->
           <!-- Include Font Awesome (add this in your <head> section) -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<!-- Product details -->
<div class="space-y-4 mb-6">
  <div class="bg-gray-800 rounded-lg p-4">
    <h3 class="text-white font-semibold mb-2">Product Details</h3>
    <div class="space-y-2 text-gray-300">
      <p><i class="fas fa-car"></i> <strong>Model:</strong> <%= product.model %></p>
      <p><i class="fas fa-calendar-alt"></i> <strong>Year:</strong> <%= product.year %></p>
      <p><i class="fas fa-tachometer-alt"></i> <strong>Mileage:</strong> <%= product.mileage %> km</p>
      <p><i class="fas fa-check-circle"></i> <strong>Condition:</strong> <%= product.condition %></p>
      <p><i class="fas fa-car-side"></i> <strong>Body Type:</strong> <%= product.bodyType %></p>
      <p><i class="fas fa-cogs"></i> <strong>Transmission:</strong> <%= product.transmission %></p>
      <p><i class="fas fa-gas-pump"></i> <strong>Fuel Type:</strong> <%= product.fuelType %></p>
      <p><i class="fas fa-comments"></i> <strong>Negotiable:</strong> <%= product.negotiable %></p>
      
      <div>
        <strong><i class="fas fa-cogs"></i> Features:</strong>
        <ul class="list-disc list-inside space-y-1">
          <% Object.entries(product.features).forEach(([feature, value]) => { %>
            <li><i class="fas fa-check"></i> <%= feature.charAt(0).toUpperCase() + feature.slice(1) %>: <%= value %></li>
          <% }) %>
        </ul>
      </div>
    </div>
  </div>
</div>

            <!-- Add to Cart Button -->
            <div class="mt-6">
              <button 
                class="w-full h-14 px-6 py-2 font-semibold rounded-xl bg-indigo-600 hover:bg-indigo-500 text-white transition-colors flex items-center justify-center gap-2"
                id="add-to-cart-button"
                data-product-id="<%= product._id %>"
                    <% if (locals.user && user.username) { %>
                      data-user-id="<%= user.username %>"
                    <% } %>
              >
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
                </svg>
                Add to Cart
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    
     
    </div>

    <!-- Add to Cart Script -->

    <script>
                  document.getElementById('add-to-cart-button').addEventListener('click', function (event) {
                    const productId = event.target.getAttribute('data-product-id');
                    const userId = event.target.getAttribute('data-user-id');

                    fetch(`/add-to-cart/${productId}`, {
                      method: 'POST',
                      headers: {
                        'Content-Type': 'application/json'
                      }
                    })
                      .then(response => response.json())
                      .then(data => {
                        if (data.message) {
                          alert(data.message);
                          if (userId && data.message!='User is not a Visitor') {
                            window.location.href = `/cart/${userId}`;
                          }
                          else{
                            window.location.href = `/login`;
                          }
                          
                        }
                      })
                      .catch((error) => {
                        console.error('Error:', error);
                      });
                  });
                </script>
</body>

</html>